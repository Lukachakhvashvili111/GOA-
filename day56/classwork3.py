def cube():
      length = int(input('type the cubes length : '))
      width = int(input('type the cubes width : '))
      length = length * 2
      width = width * 2
      return(length + width)

print(cube())